'use strict'

import React from 'react'
import Button from './button'

const SearchButton = () => (
  <Button handleClick={() => console.log('searching..')}>Search</Button>
)

export default SearchButton
